import { ArrowRight, BookOpen, GitBranch, GitMerge, ShieldCheck, Wallet } from 'lucide-react'
import { Link } from 'react-router'

export default function Help() {
  return <div className="container page-wrap help-page"><div className="page-intro narrow"><span className="eyebrow">A READER'S GUIDE</span><h1 tabIndex={-1}>How CanonMerge works.</h1><p>A short guide to writing, the validator decision, and the 2 GEN round.</p></div><div className="help-layout"><nav className="help-toc" aria-label="On this page"><a href="#story">The story</a><a href="#decision">The decision</a><a href="#gen">GEN & recovery</a><a href="#wallet">Wallet & privacy</a></nav><div className="help-content"><section id="story"><BookOpen size={27} aria-hidden="true" /><h2>Start with one canon</h2><p>A host writes the world's starting facts and rules, invites two different wallet addresses, and funds a 2 GEN collaboration round. Each writer submits one scene from the same parent. The exact text and wallet addresses become public contract state.</p><p>The story is a fictional commitment. CanonMerge does not verify real events, copyright, or offchain publication.</p></section><section id="decision"><GitMerge size={27} aria-hidden="true" /><h2>Let validators compare meaning</h2><p>GenLayer validators examine whether both scenes can coexist in one chronology while preserving the parent's rules. Code checks that the response covers both writers and every required constraint before any result changes rights or GEN.</p><div className="help-outcomes"><div><GitMerge size={22} aria-hidden="true" /><strong>One shared canon</strong><span>Both scenes fit. The writers each gain a continuation right and 1 GEN credit.</span></div><div><GitBranch size={22} aria-hidden="true" /><strong>Two story paths</strong><span>The scenes conflict. Each scene begins its own fork, and the host receives a 2 GEN refund credit.</span></div></div></section><section id="gen"><ShieldCheck size={27} aria-hidden="true" /><h2>What happens to the 2 GEN?</h2><p>The host locks exactly 2 GEN when opening a round. A finalized merge creates two 1 GEN writer credits. A finalized conflict or eligible recovery creates one 2 GEN sponsor credit. Credits are withdrawn through a separate wallet transaction.</p><p>If a scene is missing or a review stays unresolved past the deadline, an eligible participant can recover the purse. An unavailable or ambiguous review never rewards or penalizes anyone by itself. Check the world page for the current contract state before retrying.</p></section><section id="wallet"><Wallet size={27} aria-hidden="true" /><h2>Wallet, network & public text</h2><p>Choose a detected EVM wallet in the connection dialog. The app checks GenLayer Studio Dev before any write. You can disconnect from the address menu; doing so clears the selected wallet in this page and disables write actions until you reconnect.</p><p>Every scene you submit is public. The app does not invent balances, signatures, transaction fees, or finality. It reads the contract again after a transaction finalizes.</p></section><div className="help-cta"><h2>Ready to enter a world?</h2><Link className="button button-primary" to="/worlds">Explore worlds <ArrowRight size={18} aria-hidden="true" /></Link></div></div></div></div>
}
